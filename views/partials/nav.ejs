<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/"><em><code>Spreadsheets App</code></em></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <ul class="navbar-nav ml-auto">
            <!-- <li class="nav-item <%= (ax == 1)?'active':'' %>">
                <a class="nav-link" href="/dashboard">Home</a>
            </li> -->
            <% if(typeof(r) != 'undefined'){if(r.accType == 1){ %>

            <li class="nav-item <%= (ax == 3)?'active':'' %>">
                <a class="nav-link" href="/admin">Admin Console</a>
            </li>

            <% }} %>

            <% if(typeof(r) != 'undefined'){if(r.logged){%>
            <li class="nav-item <%= (ax == 1)?'active':'' %>">
                <a class="nav-link" href="/dashboard">Dashboard</a>
            </li>

            <li class="nav-item <%= (ax == 2)?'active':'' %>">
                <a class="nav-link" href="/settings">Settings</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="/logout">Logout</a>
            </li>
            <% }else{ %>
            <li class="nav-item">
                <a class="nav-link" href="/login">Login</a>
            </li>
            <% }} %>
        </ul>

    </div>
</nav>